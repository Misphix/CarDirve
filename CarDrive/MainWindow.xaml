<Window x:Class="CarDrive.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CarDrive"
        mc:Ignorable="d"
        Title="Car Drive" Height="600" Width="800">
    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="_File">
                <MenuItem Header="_Open" />
                <MenuItem Header="_Save" />
                <MenuItem Header="_Close" />
            </MenuItem>
        </Menu>
        <local:ControlBar x:Name="ControlBar" DockPanel.Dock="Top" />
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0.5*" />
                <ColumnDefinition Width="0.5*" />
            </Grid.ColumnDefinitions>
            <local:Simulator x:Name="OriginSimulator" Grid.Column="0" BorderThickness="10" />
            <local:Simulator x:Name="ContractSimulator" Grid.Column="1" BorderThickness="10" />
        </Grid>
    </DockPanel>
</Window>

